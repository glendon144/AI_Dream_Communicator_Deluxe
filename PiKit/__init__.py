"""PiKit — OPML / knowledge-organization product (AI Dream Communicator suite).

Making PiKit an importable package (``PiKit.core``, ``PiKit.modules.*``) lets
the AI Navigator shell host it in-process as an embedded Qt6 pane while the
standalone Tk entry point (``python main.py``) keeps running from this
directory with top-level ``core`` / ``modules`` imports.
"""
