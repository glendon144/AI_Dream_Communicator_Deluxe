[project]
name = "webmcp_relay"
version = "0.2.0"
description = "Conservative local MCP relay for a published WebMCP manifest."
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
